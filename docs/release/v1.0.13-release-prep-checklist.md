# v1.0.13 Release Prep Checklist

Status: operator_checklist

## Goal

Prepare the resident daemon schema-only follow-up work for a future `v1.0.13` release decision without claiming that the release is already cut.

## Scope

- event record schema
- preflight checklist
- preflight-derived event record preview output
- state machine schema

## Release-prep checks

- [ ] Review `/Users/tomyuk/Projects/Sayane/sayane/docs/release/v1.0.13-resident-daemon-preflight-schema-preview.md`
- [ ] Review `/Users/tomyuk/Projects/Sayane/sayane/CHANGELOG.md`
- [ ] Confirm the preflight command examples still match CLI behavior
- [ ] Confirm the safety boundary wording still matches implementation reality
- [ ] Record GitHub CI result for the release-prep commit
- [ ] Confirm no text implies a production daemon implementation

## Local validation

Run at minimum:

```bash
./.venv/bin/ruff check src tests
./.venv/bin/pytest -q
```

## CI recording

Record one of:

```text
CI: success
CI: failure
CI: unconfirmed
```

Do not close release prep while CI remains unconfirmed.

Current known state:

```text
7436620951649ebeb6dcbd79aa55b2a955f0846d -> CI #668 failure (Ruff format check)
1e48291184fb44a8b7d51e4bce6f778afaa666c1 -> CI unconfirmed
```

## Version bump gate

Before editing `/Users/tomyuk/Projects/Sayane/sayane/pyproject.toml`:

- [ ] release note is reviewed
- [ ] changelog entry is reviewed
- [ ] CI status is recorded
- [ ] release boundary statements are reviewed

## Tag and publish notes

This checklist does not:

- create a git tag
- push a release tag
- create the GitHub release object
- publish to PyPI

Those should happen only after final release approval.
