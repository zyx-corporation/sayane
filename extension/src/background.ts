/**
 * Omomuki extension background service worker (Phase 3).
 * Phase 0: placeholder only — no Bridge or capture logic yet.
 */
chrome.runtime.onInstalled.addListener(() => {
  console.info("[Omomuki] extension installed (Phase 0 skeleton)");
});
