# v1.0.57 Resident App API Surface Smoke Release Note

## Summary

- added `scripts/check-resident-app-api-surfaces.sh` for bearer-backed resident
  app read surface verification
- covers overview, screen-state, daemon drill-down, and candidate queue JSON
  reads without depending on pytest collection behavior
- complements the UI session smoke added in `v1.0.56`

## Why

- `tests/test_bridge_api.py` remains slower and less predictable to iterate in
  this environment than the equivalent direct local Bridge checks
- the release line still benefits from a stable regression path for app-facing
  JSON read surfaces

## Validation

- `bash -n scripts/check-resident-app-api-surfaces.sh`
- `bash scripts/check-resident-app-api-surfaces.sh --start`
