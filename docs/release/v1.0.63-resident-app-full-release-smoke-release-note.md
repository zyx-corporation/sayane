# v1.0.63 Resident App Full Release Smoke Release Note

## Summary

- validated the full resident app release-smoke wrapper path with
  `--start --with-native`
- confirmed one command now covers bearer-backed API surfaces, UI session
  bootstrap/logout, and native macOS preview smoke
- hardened `run-app-local.sh` with one retry so wrapper-driven startup handles
  the remaining detached Bridge launch flake more reliably

## Why

- the practical release boundary is no longer API-only or browser-only; the
  native preview is part of the same operator-facing surface
- the release wrapper should be strong enough to serve as the one command an
  operator or maintainer runs before calling the current local app shell healthy

## Validation

- `bash scripts/check-resident-app-release-smoke.sh --start --with-native`
