/** @deprecated Use providers/registry — thin re-exports for existing imports. */
export {
  getProviderById as getAdapterById,
  getProviderForUrl as getAdapterForUrl,
  insertTextIntoPage,
} from "../providers/registry.js";
