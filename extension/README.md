# Omomuki Chrome Extension

Phase 0 provides a Manifest V3 skeleton only. Full capture/insert and Local Bridge pairing are planned for Phase 3.

## Development

```bash
cd extension
npm install
npm run build
```

Load unpacked extension from the `extension/` directory in Chrome (requires `dist/background.js` after build).
